name: "Sample React CI pipeline / workflow"
on:
  push:
    branches:
      - master
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: "Setup node js"
        uses: actions/setup-node@v4
        with:
          node-version: "21"